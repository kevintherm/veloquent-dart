name: veloquent_sdk
description: Flutter SDK for Veloquent BaaS with auth, records, collections, schema, onboarding, and realtime support.
version: 1.1.0
repository: https://github.com/kevintherm/veloquent-dart
topics:
  - veloquent
  - sdk
  - laravel

environment:
  sdk: ^3.10.4
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.5.0
  pusher_channels_flutter: ^2.4.0
  shared_preferences: ^2.5.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ^6.0.0
  mocktail: ^1.0.4
  test: ^1.25.6
